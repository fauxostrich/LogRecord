/**
 * You can run the logRecord program through main.js
 * and all info is exported and displayed to console here.
 */
let ref = require('./logRecord');
debugger; // to see if ref has the value passed from readLineSync.js
console.log(ref.jsonArr);
